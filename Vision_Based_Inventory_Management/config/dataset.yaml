# config/dataset.yaml
path: ./data/SKU-110K  # dataset root dir
train: train.txt
val: val.txt
test: test.txt

# Classes - SKU-110K is single class (object detection)
names:
  0: product

# Training parameters
nc: 1  # number of classes